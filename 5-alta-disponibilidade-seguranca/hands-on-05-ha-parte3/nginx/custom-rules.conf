# ---------------------------------------------------------
# 🔹 Regra 1001 - Detectar SQL Injection
# - @detectSQLi → usa o operador nativo do ModSecurity para identificar padrões de SQL Injection
# - ARGS → inspeciona todos os parâmetros da requisição (?id=1, ?user=admin, etc.)
# - phase:2 → verifica o corpo da requisição depois do parsing do request
# - block → bloqueia imediatamente se detectar ataque
# - severity: CRITICAL → nível de severidade do evento
# ---------------------------------------------------------
SecRule ARGS "@detectSQLi" \
    "id:1001,\
    phase:2,\
    block,\
    msg:'SQL Injection Attack Detected',\
    severity:'CRITICAL'"

# ---------------------------------------------------------
# 🔹 Regra 1002 - Detectar Cross-Site Scripting (XSS)
# - @detectXSS → operador nativo para encontrar payloads de XSS (<script>, onload, javascript:)
# - ARGS → verifica parâmetros enviados na URL ou no corpo
# - phase:2 → inspeção do request body
# - block → bloqueia se identificado
# - severity: CRITICAL → ataque de alta gravidade
# ---------------------------------------------------------
SecRule ARGS "@detectXSS" \
    "id:1002,\
    phase:2,\
    block,\
    msg:'XSS Attack Detected',\
    severity:'CRITICAL'"

# ---------------------------------------------------------
# 🔹 Regra 1003 - Bloquear User-Agents maliciosos
# - REQUEST_HEADERS:User-Agent → verifica o cabeçalho User-Agent do cliente
# - @pmFromFile /etc/modsecurity.d/malicious-ua.txt → compara contra lista de UAs conhecidos (sqlmap, nikto, etc.)
# - phase:1 → executa bem no início do request
# - block → bloqueia imediatamente
# - severity: WARNING → menos crítico que SQLi/XSS, mas ainda um indicador de ataque
# ---------------------------------------------------------
SecRule REQUEST_HEADERS:User-Agent "@pmFromFile /etc/modsecurity.d/malicious-ua.txt" \
    "id:1003,\
    phase:1,\
    block,\
    msg:'Malicious User Agent Detected',\
    severity:'WARNING'"

# ---------------------------------------------------------
# 🔹 Regra 1004 - Rate Limiting customizado por IP
# - IP:REQUEST_COUNT → contador de requisições por endereço IP
# - @gt 100 → dispara se mais de 100 requisições forem feitas em curto período
# - phase:1 → analisa logo no começo do processamento
# - block → bloqueia após atingir o limite
# - severity: WARNING → indica abuso ou tentativa de DoS
# ---------------------------------------------------------
SecRule IP:REQUEST_COUNT "@gt 100" \
    "id:1004,\
    phase:1,\
    block,\
    msg:'Rate Limit Exceeded',\
    severity:'WARNING'"
