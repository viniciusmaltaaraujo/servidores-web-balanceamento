# Configurações globais de eventos (quantas conexões simultâneas o worker aceita)
events {
    worker_connections 1024;   # Cada worker pode lidar com até 1024 conexões
}

http {
    # Inclui tipos MIME para servir arquivos (html, css, js, etc.)
    include /etc/nginx/mime.types;
    default_type application/octet-stream;   # Tipo padrão caso não seja identificado

    # -------------------------------
    # 🔹 Definição das ZONAS de rate limiting
    # Essas zonas armazenam os IPs e contadores de requisições
    # Só podem ser declaradas aqui dentro do bloco http{}
    # -------------------------------
    limit_req_zone $binary_remote_addr zone=api:10m rate=1r/s;     # Máx. 1 req/s para /api/
    limit_req_zone $binary_remote_addr zone=login:10m rate=1r/s;   # Máx. 1 req/s para /login

    # -------------------------------
    # 🔹 Logs customizados
    # -------------------------------
    log_format security '$remote_addr - $remote_user [$time_local] '
                        '"$request" $status $body_bytes_sent '
                        '"$http_referer" "$http_user_agent" rt=$request_time';

    access_log /var/log/nginx/access.log security;   # Log de acessos usando formato acima
    error_log /var/log/nginx/error.log warn;        # Log de erros (nível warn)

    server {
        listen 80;                # Porta padrão HTTP
        server_name localhost;    # Nome do servidor
        root /var/www/html;       # Pasta raiz dos arquivos
        index index.html index.php; # Arquivos de índice

        # -------------------------------
        # 🔹 Rota /api/
        # - Aplica rate limit definido na zona "api"
        # - burst=2 → permite até 2 excessos antes de bloquear
        # - nodelay → bloqueia imediatamente se ultrapassar
        # -------------------------------
        location /api/ {
            limit_req zone=api burst=2 nodelay;
            proxy_pass http://api:5000; 
        }

        # -------------------------------
        # 🔹 Rota /login
        # - Aplica regras de rate limit diferentes
        # - burst=3 → tolera até 3 excessos
        # -------------------------------
        location /login {
            limit_req zone=login burst=3 nodelay;
            try_files $uri $uri/ =404;
        }

        # -------------------------------
        # 🔹 Regras de bloqueio via expressões regulares
        # -------------------------------

        # Bloqueia scanners conhecidos via User-Agent
        if ($http_user_agent ~* (sqlmap|nikto|nmap|masscan|nessus)) {
            return 403;   # Forbidden
        }

        # Bloqueia SQL Injection simples (union, select, drop, etc.)
        if ($args ~* (union|select|insert|delete|drop|create|alter|exec|script)) {
            return 403;
        }

        # Bloqueia tentativas de XSS básico
        if ($args ~* (<script|javascript:|vbscript:|onload|onerror)) {
            return 403;
        }

        # -------------------------------
        # 🔹 Rota padrão /
        # -------------------------------
        location / {
            try_files $uri $uri/ =404;   # Tenta servir arquivo, senão 404
        }

        # -------------------------------
        # 🔹 Simulação de PHP
        # Aqui o Nginx não executa PHP de verdade, apenas retorna texto
        # -------------------------------
        location ~ \.php$ {
            add_header Content-Type text/html;
            return 200 "PHP processado: $request_uri";
        }
    }
}
